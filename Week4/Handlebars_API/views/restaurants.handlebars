<div class="promo-blocks">
  {{#if restaurants.length }}
    {{#each restaurants }}
      {{> restaurantItem this }}
    {{/each}}
  {{else}}
    <p>No restaurants left :(</p>
    <a href="/">Go Back</a>
  {{/if}}
</div>